param([int]$BasePort = 9360,[string]$Only = '',[switch]$SkipSale,[switch]$SkipProbes,[switch]$PricelistWorkflow,[switch]$LoyaltyWorkflow,[switch]$LoyaltyPersistence,[switch]$LoyaltyHistory,[switch]$LoyaltyRefundWorkflow,[string]$RefundExistingReference = '',[switch]$AccountLoyaltySeparation,[string]$DualEmail = '',[string]$DualPassword = '')
$ErrorActionPreference='Stop'
$edge='C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe'
$cases=@(
  @{name='MINIMAL';id=4;pin='5678';email='mohamed@shifo.so';password='Pos@5678Secure'},
  @{name='BASIC';id=3;pin='1234';email='fartun@shifo.so';password='Pos@1234Secure'},
  @{name='ADVANCED';id=6;pin='3456';email='khadija@shifo.so';password='Pos@3456Secure'}
)
if($DualEmail -and $DualPassword){
  $cases+=@{name='BASIC_REOPEN';id=3;pin='1234';email='fartun@shifo.so';password='Pos@1234Secure'}
  $cases+=@{name='DUAL_MINIMAL';id=4;pin='5678';email=$DualEmail;password=$DualPassword}
}
if($Only){$cases=@($cases|Where-Object{$_.name -eq $Only});if(!$cases.Count){throw "Unknown role case: $Only"}}

function Invoke-RoleCase($case,[int]$port) {
  $profile=Join-Path $env:TEMP "curdun-p148-role-$($case.name)-$port"
  $url='http://localhost:8000/app.html'
  $proc=Start-Process -FilePath $edge -ArgumentList @('--headless','--disable-gpu','--disable-extensions','--no-first-run','--remote-allow-origins=*',"--remote-debugging-port=$port","--user-data-dir=$profile",$url) -WindowStyle Hidden -PassThru
  $ws=$null
  try {
    $page=$null
    for($i=0;$i -lt 50 -and !$page;$i++){
      Start-Sleep -Milliseconds 200
      try { foreach($candidate in (Invoke-RestMethod "http://localhost:$port/json/list")){if($candidate.type -eq 'page' -and $candidate.url -like 'http://localhost:8000/*'){$page=$candidate;break}} } catch {}
    }
    if(!$page){throw "No browser page for $($case.name)"}
    $ws=[Net.WebSockets.ClientWebSocket]::new();$ws.ConnectAsync([Uri]$page.webSocketDebuggerUrl,[Threading.CancellationToken]::None).GetAwaiter().GetResult()|Out-Null
    $script=@"
(async()=>{
 const wait=async(fn,label)=>{for(let i=0;i<100;i++){const v=fn();if(v)return v;await new Promise(r=>setTimeout(r,100));}throw new Error('Timeout '+label)};
 const fill=(el,value)=>{el.value=value;el.dispatchEvent(new Event('input',{bubbles:true}))};
 const entry=await wait(()=>document.querySelector('#login-email')||document.querySelector('[data-launch="pos"]'),'account state');if(entry.id==='login-email'){fill(entry,'$($case.email)');fill(document.querySelector('#login-pw'),'$($case.password)');document.querySelector('#btn-signin').click();await wait(()=>document.querySelector('[data-launch="pos"]'),'workspace POS');}document.querySelector('[data-launch="pos"]').click();
 const retail=await wait(()=>document.querySelector('[data-store-type="retail"]')||document.querySelector('#btn-topnav-user'),'store selector');if(retail.matches?.('[data-store-type]'))retail.click();
 await wait(()=>document.querySelector('#btn-topnav-user'),'POS navigation');await new Promise(r=>setTimeout(r,300));document.querySelector('#btn-topnav-user').click();
 await wait(()=>document.querySelector('#btn-bo-lock'),'lock control');await new Promise(r=>setTimeout(r,300));document.querySelector('#btn-bo-lock').click();await wait(()=>!S.posActiveUser,'locked state');
 await new Promise(r=>setTimeout(r,1000));await wait(()=>document.querySelector('[data-login-id="$($case.id)"]'),'staff card');
 document.querySelector('[data-login-id="$($case.id)"]').click();await new Promise(r=>setTimeout(r,300));
 let entered=0;for(const digit of '$($case.pin)'){const key=await wait(()=>[...document.querySelectorAll('[data-key]')].find(b=>b.dataset.key===digit),'PIN key');await new Promise(r=>setTimeout(r,100));key.click();entered++;if(entered<4)await wait(()=>S.posLoginPin.length===entered,'PIN digit '+entered);}
 for(let i=0;i<100&&!S.posActiveUser;i++)await new Promise(r=>setTimeout(r,100));if(!S.posActiveUser)throw new Error('PIN login failed: error='+S.posAuthError+'; pin='+S.posLoginPin+'; selected='+S._loginSelectedId+'; view='+S.view);await new Promise(r=>setTimeout(r,300));
 const api=async(path,method='GET',body)=>{const r=await fetch('/api/v1'+path,{method,credentials:'include',headers:{'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body)});let data=null;try{data=await r.json()}catch(_){}return{status:r.status,message:data?.message||null}};
 const caps={sell:posCan('pos.sell'),orders:posCan('pos.orders_view'),reports:posCan('pos.reports_view'),pricelist:posCan('pos.pricelist_select'),refund:posCan('pos.refund'),cash:posCan('pos.cash_in'),open:posCan('pos.register_open'),close:posCan('pos.register_close'),loyalty:posCan('pos.loyalty_operate')};
 S.posView='session';S.posTab='checkout';render();await new Promise(r=>setTimeout(r,150));
 const checkout={screen:!!document.querySelector('.pos-checkout-layout'),pricelist_disabled:document.querySelector('#pos-pricelist-select')?.disabled??null,reward_disabled:document.querySelector('#btn-pos-rewards')?.disabled??null,open_button:!!document.querySelector('#btn-checkout-open-register')};
 let pricelistWorkflow=null;if($($PricelistWorkflow.IsPresent.ToString().ToLower())){
   const quoteAt=async qty=>{await wait(()=>S.posCart[0]?.qty===qty&&!S.posQuoteLoading&&Number(S.posQuote?.lines?.[0]?.qty)===qty,'quote qty '+qty);return{qty,unit:Number(S.posQuote.lines[0].final_unit_price),label:document.querySelector('.pos-cart-item-price')?.textContent.trim(),pricelist:document.querySelector('#pos-pricelist-select')?.selectedOptions[0]?.textContent.trim()}};
   const setQty=async qty=>{while(Number(S.posCart[0]?.qty||0)<qty){const before=Number(S.posCart[0].qty);await new Promise(r=>setTimeout(r,150));document.querySelector('[data-qty-plus="0"]').click();await wait(()=>Number(S.posCart[0]?.qty)===before+1,'qty plus')}while(Number(S.posCart[0]?.qty||0)>qty){const before=Number(S.posCart[0].qty);await new Promise(r=>setTimeout(r,150));document.querySelector('[data-qty-minus="0"]').click();await wait(()=>Number(S.posCart[0]?.qty)===before-1,'qty minus')}return quoteAt(qty)};
   await new Promise(r=>setTimeout(r,350));const initialPricelist=document.querySelector('#pos-pricelist-select').selectedOptions[0].textContent.trim();document.querySelector('[data-add-product="1"]').click();await wait(()=>S.posCart.length===1,'visible product add');
   const standard9=await setQty(9),standard10=await setQty(10);
   const priceSelect=document.querySelector('#pos-pricelist-select');priceSelect.value='6';priceSelect.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posSelectedPricelistId)===6&&!S.posQuoteLoading,'Promotion selection');
   const promotion9=await setQty(9),promotion10=await setQty(10),promotion9Again=await setQty(9);
   const customerSelect=document.querySelector('#pos-deyn-customer');customerSelect.value='44';customerSelect.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posDebtCustomerId)===44&&Number(S.posSelectedPricelistId)===6&&!S.posQuoteLoading,'preferred Promotion');const preferred={customer:customerSelect.selectedOptions[0].textContent,pricelist:document.querySelector('#pos-pricelist-select').selectedOptions[0].textContent.trim(),unit:Number(S.posQuote.lines[0].final_unit_price)};
   customerSelect.value='45';customerSelect.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posDebtCustomerId)===45&&Number(S.posSelectedPricelistId)===2&&!S.posQuoteLoading,'unavailable fallback');const fallback={customer:customerSelect.selectedOptions[0].textContent,pricelist:document.querySelector('#pos-pricelist-select').selectedOptions[0].textContent.trim(),unit:Number(S.posQuote.lines[0].final_unit_price)};
   pricelistWorkflow={initialPricelist,standard9,standard10,promotion9,promotion10,promotion9Again,preferred,fallback};
 }
 let loyaltyWorkflow=null;if($($LoyaltyWorkflow.IsPresent.ToString().ToLower())){
   await new Promise(r=>setTimeout(r,350));document.querySelector('[data-add-product="1"]').click();await wait(()=>S.posCart.length===1&&S.posQuote&&!S.posQuoteLoading,'loyalty cart');
   const customerSelect=document.querySelector('#pos-deyn-customer');customerSelect.value='30';customerSelect.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posDebtCustomerId)===30&&S.posCustomerLoyalty?.customer&&!S.posQuoteLoading,'eligible customer');
   const before={points:Number(S.posCustomerLoyalty.customer.loyalty_points),debt:Number(POS_CUSTOMERS.find(c=>c.id===30).debtBalance),total:Number(S.posQuote.total)};const rewardsButton=document.querySelector('#btn-pos-rewards');const rewardsButtonText=rewardsButton.textContent.trim();rewardsButton.click();await wait(()=>document.querySelector('[data-loyalty-reward]'),'visible rewards');
   const visibleRewards=[...document.querySelectorAll('[data-loyalty-reward]')].map(b=>({id:Number(b.dataset.loyaltyReward),card:b.parentElement.textContent.replace(/\s+/g,' ').trim()}));await new Promise(r=>setTimeout(r,300));const rewardButton=document.querySelector('[data-loyalty-reward]');const selectedId=Number(rewardButton.dataset.loyaltyReward);rewardButton.click();await wait(()=>Number(S.posSelectedRewardId)===selectedId&&S.posQuote?.loyalty?.selected_reward&&!S.posQuoteLoading,'server reward quote');
   const selected={id:selectedId,name:S.posQuote.loyalty.selected_reward.name,cost:Number(S.posQuote.loyalty.redeemed_points),projected:Number(S.posQuote.loyalty.projected_points),discount:Number(S.posQuote.discounts.loyalty),total:Number(S.posQuote.total)};
   await new Promise(r=>setTimeout(r,250));[...document.querySelectorAll('[data-add-method]')].find(b=>b.dataset.addMethod==='Cash').click();await wait(()=>!document.querySelector('#btn-pos-charge').disabled,'loyalty Validate');document.querySelector('#btn-pos-charge').click();await wait(()=>S.posReceiptVisible&&S.posLastReceipt,'loyalty receipt');const receipt={reference:S.posLastReceipt.id,total:S.posLastReceipt.total,reward:S.posLastReceipt.loyaltyRewardName,redeemed:S.posLastReceipt.loyaltyPointsRedeemed,earned:S.posLastReceipt.loyaltyPointsEarned};const customerAfter=POS_CUSTOMERS.find(c=>c.id===30);const after={points:Number(customerAfter.points),debt:Number(customerAfter.debtBalance)};
   S.posReceiptVisible=false;S.posView='session';S.posTab='checkout';render();await new Promise(r=>setTimeout(r,300));document.querySelector('[data-add-product="1"]').click();await wait(()=>S.posCart.length===1&&S.posQuote&&!S.posQuoteLoading,'insufficient cart');const lowSelect=document.querySelector('#pos-deyn-customer');lowSelect.value='31';lowSelect.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posDebtCustomerId)===31&&S.posCustomerLoyalty?.customer&&!S.posQuoteLoading,'insufficient customer');const insufficient={points:Number(S.posCustomerLoyalty.customer.loyalty_points),eligible:Number(S.posQuote.loyalty.eligible_rewards.length),buttonDisabled:document.querySelector('#btn-pos-rewards').disabled};
   loyaltyWorkflow={customer:'P10 CustA',before,rewardsButtonText,visibleRewards,selected,receipt,after,insufficient};
 }
 let loyaltyPersistence=null;if($($LoyaltyPersistence.IsPresent.ToString().ToLower())){await new Promise(r=>setTimeout(r,300));document.querySelector('[data-add-product="1"]').click();await wait(()=>S.posCart.length===1&&S.posQuote&&!S.posQuoteLoading,'persistence cart');const cs=document.querySelector('#pos-deyn-customer');cs.value='30';cs.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posDebtCustomerId)===30&&S.posCustomerLoyalty?.customer&&!S.posQuoteLoading,'persisted loyalty');loyaltyPersistence={pill:document.body.textContent.match(/Loyalty\s+([\d.]+) pts/)?.[1]||null,points:Number(S.posCustomerLoyalty.customer.loyalty_points),ledger:(S.posCustomerLoyalty.entries||[]).slice(0,4).map(r=>({order_id:r.order_id,type:r.type,points:Number(r.points),balance_after:Number(r.balance_after)})),invariant_ok:Boolean(S.posCustomerLoyalty.invariant_ok),debt:Number(POS_CUSTOMERS.find(c=>c.id===30).debtBalance)};}
 let loyaltyHistory=null;if($($LoyaltyHistory.IsPresent.ToString().ToLower())){const saleRef='POS-20260822-01-000189';S.posView='backoffice';S.posBackofficeTab='orders';render();await new Promise(r=>setTimeout(r,350));const detailButton=await wait(()=>document.querySelector('[data-view-txn-btn="'+saleRef+'"]'),'historical loyalty order');detailButton.click();await wait(()=>S.viewModal?.id===saleRef,'historical detail state');render();const modal=await wait(()=>document.querySelector('.crud-modal'),'historical detail modal');const txn=POS_TRANSACTIONS.find(t=>t.id===saleRef);const visible=modal.textContent.replace(/\s+/g,' ').trim();if(!visible.includes('Loyalty redeemed'))throw new Error('Historical loyalty snapshot missing: '+JSON.stringify({txn,visible}));loyaltyHistory={saleRef,earned:Number(txn.loyaltyPointsEarned),redeemed:Number(txn.loyaltyPointsRedeemed),rewardId:txn.loyaltyRewardId,discount:Number(txn.loyaltyDiscount),visible};}
 let loyaltyRefundWorkflow=null;if($($LoyaltyRefundWorkflow.IsPresent.ToString().ToLower())){const existingRef='$RefundExistingReference';let saleRef=existingRef,saleEarned=null,pointsBefore=null;if(!existingRef){await new Promise(r=>setTimeout(r,300));document.querySelector('[data-add-product="1"]').click();await wait(()=>S.posCart.length===1&&S.posQuote&&!S.posQuoteLoading,'earning cart');const cs=document.querySelector('#pos-deyn-customer');cs.value='31';cs.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posDebtCustomerId)===31&&S.posCustomerLoyalty?.customer&&!S.posQuoteLoading,'earning customer');pointsBefore=Number(S.posCustomerLoyalty.customer.loyalty_points);await new Promise(r=>setTimeout(r,250));[...document.querySelectorAll('[data-add-method]')].find(b=>b.dataset.addMethod==='Cash').click();await wait(()=>!document.querySelector('#btn-pos-charge').disabled,'earning Validate');document.querySelector('#btn-pos-charge').click();await wait(()=>S.posReceiptVisible&&S.posLastReceipt,'earning receipt');saleRef=S.posLastReceipt.id;saleEarned=Number(S.posLastReceipt.loyaltyPointsEarned);await wait(()=>POS_TRANSACTIONS.some(t=>t.id===saleRef),'sale transaction');S.posReceiptVisible=false;}S.posView='backoffice';S.posBackofficeTab='orders';render();await wait(()=>POS_TRANSACTIONS.some(t=>t.id===saleRef),'existing sale transaction');const saleTxn=POS_TRANSACTIONS.find(t=>t.id===saleRef);saleEarned=saleEarned??Number(saleTxn.loyaltyPointsEarned||saleTxn.loyalty_points_earned||0);await new Promise(r=>setTimeout(r,350));const refundButton=[...document.querySelectorAll('[data-refund-txn]')].find(b=>b.dataset.refundTxn===saleRef);if(!refundButton)throw new Error('Visible refund button missing for '+saleRef);refundButton.click();await wait(()=>document.querySelector('.pos-refund-qty'),'refund modal');await new Promise(r=>setTimeout(r,250));const qty=document.querySelector('.pos-refund-qty');qty.value='1';qty.dispatchEvent(new Event('input',{bubbles:true}));await wait(()=>!document.querySelector('.pos-refund-add-method[data-add-method="Cash"]').disabled,'refund Cash');await new Promise(r=>setTimeout(r,250));document.querySelector('.pos-refund-add-method[data-add-method="Cash"]').click();await new Promise(r=>setTimeout(r,500));const refundSubmit=document.querySelector('#btn-refund-submit');if(refundSubmit.disabled)throw new Error('Refund invalid: '+JSON.stringify({lines:S.posRefundModal.lines.map(l=>({qty:l.refund_qty,total:l.item.total,original_qty:l.item.original_qty,refundable_qty:l.item.refundable_qty})),payments:S.posRefundModal.payments,notice:[...document.querySelectorAll('.crud-modal-body div')].map(e=>e.textContent.trim()).find(t=>t.startsWith('Refund payments must')||t.startsWith('Add at least')||t.startsWith('Select at least'))||null}));refundSubmit.click();await wait(()=>S.posReceiptVisible&&S.posLastReceipt?.isRefund,'refund receipt');const refundRef=S.posLastReceipt.id;S.posReceiptVisible=false;S.posView='session';S.posTab='checkout';render();await new Promise(r=>setTimeout(r,300));document.querySelector('[data-add-product="1"]').click();await wait(()=>S.posCart.length===1&&!S.posQuoteLoading,'post refund cart');const post=document.querySelector('#pos-deyn-customer');post.value='31';post.dispatchEvent(new Event('change',{bubbles:true}));await wait(()=>Number(S.posDebtCustomerId)===31&&S.posCustomerLoyalty?.customer&&!S.posQuoteLoading,'post refund loyalty');const pointsAfter=Number(S.posCustomerLoyalty.customer.loyalty_points);loyaltyRefundWorkflow={pointsBefore,saleRef,saleEarned,refundRef,pointsAfter,entries:(S.posCustomerLoyalty.entries||[]).slice(0,3).map(r=>({order_id:r.order_id,type:r.type,points:Number(r.points),balance_after:Number(r.balance_after)}))};}
 let accountLoyaltySeparation=null;if($($AccountLoyaltySeparation.IsPresent.ToString().ToLower())){const customerId=30,amount=0.10;const loyaltyBefore=await posLoadCustomerLoyalty(customerId);S.posView='backoffice';S.posBackofficeTab='customers';render();await new Promise(r=>setTimeout(r,350));const accountButton=await wait(()=>document.querySelector('[data-account-customer="30"]'),'customer account control');accountButton.click();await wait(()=>S.posCustomerAccount?.data&&!S.posCustomerAccount.loading,'customer ledger');const debtBefore=Number(S.posCustomerAccount.data.customer.balance);await new Promise(r=>setTimeout(r,350));document.querySelector('#btn-account-payment-open').click();await wait(()=>document.querySelector('#account-payment-amount'),'settlement form');await new Promise(r=>setTimeout(r,350));fill(document.querySelector('#account-payment-amount'),String(amount));document.querySelector('#btn-account-payment-submit').click();await wait(()=>S.posCustomerAccount?.data&&!S.posCustomerAccount.settleOpen&&!S.posCustomerAccount.submitting,'settlement complete');const loyaltyAfter=await posLoadCustomerLoyalty(customerId);accountLoyaltySeparation={customer:S.posCustomerAccount.data.customer.name,amount,method:'Cash',debtBefore,debtAfter:Number(S.posCustomerAccount.data.customer.balance),pointsBefore:Number(loyaltyBefore.customer.loyalty_points),pointsAfter:Number(loyaltyAfter.customer.loyalty_points),loyaltyInvariant:Boolean(loyaltyAfter.invariant_ok),latestLedger:S.posCustomerAccount.data.entries?.[0]||null};}
 let dualSale=null;if('$($case.name)'==='DUAL_MINIMAL'&&!$($SkipSale.IsPresent.ToString().ToLower())){await new Promise(r=>setTimeout(r,300));document.querySelector('[data-add-product="1"]').click();await wait(()=>S.posCart.length===1&&S.posQuote&&!S.posQuoteLoading,'quoted visible cart');await new Promise(r=>setTimeout(r,300));[...document.querySelectorAll('[data-add-method]')].find(b=>b.dataset.addMethod==='Cash').click();await wait(()=>!document.querySelector('#btn-pos-charge').disabled,'enabled Validate');document.querySelector('#btn-pos-charge').click();await wait(()=>S.posReceiptVisible&&S.posLastReceipt,'visible receipt');dualSale={reference:S.posLastReceipt.id,total:S.posLastReceipt.total};}
 S.posView='backoffice';S.posBackofficeTab='sessions';render();await new Promise(r=>setTimeout(r,150));
 const sessions={cash_in:!!document.querySelector('#btn-cash-in'),cash_out:!!document.querySelector('#btn-cash-out'),close:!!document.querySelector('#btn-session-close'),open:!!document.querySelector('#btn-session-open')};
 document.querySelector('[data-nav-menu="orders"]')?.click();await new Promise(r=>setTimeout(r,200));document.querySelector('[data-bo-tab="orders"]')?.click();await new Promise(r=>setTimeout(r,200));
 const orders={screen:document.body.textContent.includes('Transactions')||document.body.textContent.includes('Orders'),refund:!!document.querySelector('[data-refund-txn]')};
 document.querySelector('[data-nav-menu="reporting"]')?.click();await new Promise(r=>setTimeout(r,200));document.querySelector('[data-bo-tab="reports-orders"]')?.click();await new Promise(r=>setTimeout(r,200));
 const reports={screen:document.body.textContent.includes('Orders')&&!!document.querySelector('[data-nav-menu="reporting"]')};
 let probes={};if(!$($SkipProbes.IsPresent.ToString().ToLower()))probes={transactions:await api('/pos/transactions'),reports:await api('/pos/reports'),settings:await api('/pos/settings'),manual_pricelist:await api('/pos/quote','POST',{pricelist_id:6,items:[{product_id:1,quantity:1}],payments:[]}),refund:await api('/pos/orders/0/refund','POST',{idempotency_key:crypto.randomUUID(),reason:'authorization probe'}),cash:await api('/pos/sessions/0/cash-movements','POST',{idempotency_key:crypto.randomUUID(),type:'IN',amount:1,reason:'authorization probe'}),open:await api('/pos/sessions/open','POST',{config_id:0,opening_cash:0}),close:await api('/pos/sessions/0/close','POST',{counted_cash:0}),loyalty:await api('/pos/quote','POST',{customer_id:1,loyalty_reward_id:1,items:[{product_id:1,quantity:1}],payments:[]})};
 return {role:'$($case.name)',account:'$($case.email)',employee:S.posActiveUser.name,level:posLevelForUser(S.posActiveUser),register_id:S.posSession?.id||null,caps,visible:{checkout,sessions,orders,reports},pricelistWorkflow,loyaltyWorkflow,loyaltyPersistence,loyaltyHistory,loyaltyRefundWorkflow,accountLoyaltySeparation,dualSale,probes};
})()
"@
    $msg=@{id=1;method='Runtime.evaluate';params=@{expression=$script;awaitPromise=$true;returnByValue=$true}}|ConvertTo-Json -Depth 8 -Compress
    $bytes=[Text.Encoding]::UTF8.GetBytes($msg);$ws.SendAsync([ArraySegment[byte]]::new($bytes),[Net.WebSockets.WebSocketMessageType]::Text,$true,[Threading.CancellationToken]::None).GetAwaiter().GetResult()|Out-Null
    while($true){$buf=New-Object byte[] 1048576;$recv=$ws.ReceiveAsync([ArraySegment[byte]]::new($buf),[Threading.CancellationToken]::None).GetAwaiter().GetResult();$response=([Text.Encoding]::UTF8.GetString($buf,0,$recv.Count)|ConvertFrom-Json);if($response.id -eq 1){if($response.result.exceptionDetails){throw($response.result.exceptionDetails|ConvertTo-Json -Depth 8)};return $response.result.result.value}}
  } finally {if($ws){$ws.Dispose()};if($proc -and !$proc.HasExited){Stop-Process -Id $proc.Id -Force}}
}

$results=@();for($i=0;$i -lt $cases.Count;$i++){$results+=Invoke-RoleCase $cases[$i] ($BasePort+$i)}
$results|ConvertTo-Json -Depth 12
